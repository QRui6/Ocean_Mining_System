# 🎯 使用本地数据 - 快速指南

## 问题说明

NOAA 服务器从中国大陆无法访问，所以我们改用**本地示例数据**。

---

## ✅ 已完成的修改

1. ✅ 禁用了 NOAA 自动下载
2. ✅ 保留了 NOAA API（如果将来需要可以手动触发）
3. ✅ 前端配置保持不变

---

## 🚀 现在可以做什么

### 1. 重启后端服务

停止当前服务（Ctrl+C），然后：

```bash
cd backend
npm start
```

现在应该看到：
```
🚀 服务器启动成功: http://localhost:5678
📡 WebSocket服务: ws://localhost:8081

💡 提示：NOAA 实时数据下载已禁用
   原因：NOAA 服务器从中国大陆无法直接访问
   当前使用：本地示例数据（public/data/）
```

**不会再有网络错误了！** ✅

---

### 2. 使用现有的风场数据

你的项目中已经有这些数据：

```
public/data/
├── wind_data_0701.json          ← 全球风场数据
├── wind-sample.json             ← 示例风场数据
└── wind_timeline_optimized/     ← 时序风场数据
    ├── 2024-07-01_00.json
    ├── 2024-07-01_03.json
    └── ...
```

这些数据可以直接用于演示！

---

### 3. 前端使用本地数据

前端代码已经支持加载本地数据，无需修改。

#### 方式一：使用现有的风场图层

1. 启动前端：`npm run dev`
2. 打开浏览器：http://localhost:5173
3. 点击 "矿区管理" 选项卡
4. 打开 "图层控制" 面板
5. 激活 "环境监测" → "近日风场预报"

这会加载 `public/data/wind_data_0701.json`

#### 方式二：使用 NOAA 图层（需要手动准备数据）

如果你想测试 NOAA 图层：

1. 手动下载 GRIB2 数据（使用代理或在海外服务器）
2. 转换为 JSON 格式
3. 放到 `public/data/noaa/` 目录
4. 前端就可以加载了

---

## 🌐 未来的选项

### 选项 1：部署到海外服务器

如果你有海外服务器（AWS、Azure、阿里云国际等）：

1. 部署后端到海外
2. 取消 `server.js` 中的注释
3. NOAA 数据就可以正常下载了

### 选项 2：配置代理

如果你有代理服务器，修改 `noaaDataService.js`：

```javascript
const response = await axios({
    method: 'GET',
    url: url,
    responseType: 'arraybuffer',
    timeout: 60000,
    proxy: {
        host: 'your-proxy-host',
        port: 1080
    }
});
```

### 选项 3：使用国内气象数据源

集成中国气象局或其他国内数据源。

---

## 📊 当前功能状态

| 功能 | 状态 | 说明 |
|------|------|------|
| 后端服务 | ✅ 正常 | 无网络错误 |
| 本地风场数据 | ✅ 可用 | wind_data_0701.json |
| 时序风场数据 | ✅ 可用 | wind_timeline_optimized/ |
| NOAA 实时数据 | ⏸️ 已禁用 | 需要代理或海外服务器 |
| 前端显示 | ✅ 正常 | 可以显示本地数据 |

---

## 🎯 推荐操作

### 立即可以做的：

1. **重启后端**（不会再有错误）
2. **启动前端**
3. **测试现有的风场图层**

### 演示时：

使用现有的本地数据就足够展示功能了！

### 生产环境：

考虑部署到海外服务器或使用国内数据源。

---

## ❓ 常见问题

### Q: 为什么不能访问 NOAA？

A: NOAA 服务器在美国，从中国大陆访问被防火墙阻断。这是正常现象。

### Q: 本地数据够用吗？

A: 对于演示和开发完全够用！数据格式和真实数据一样。

### Q: 如何更新本地数据？

A: 
1. 在海外服务器下载 NOAA 数据
2. 或使用代理下载
3. 或使用其他数据源

### Q: 前端会报错吗？

A: 不会！前端会尝试加载数据，如果失败会静默处理。

---

## ✅ 总结

**好消息**：
- ✅ 后端不会再报网络错误
- ✅ 可以使用本地示例数据
- ✅ 前端功能正常
- ✅ 演示完全没问题

**现在就重启后端试试吧！** 🚀

```bash
cd backend
npm start
```

应该看到干净的启动日志，没有错误！

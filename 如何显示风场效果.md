# 如何显示风场效果

## 🎯 快速开始

### 方法1：在主应用中显示

1. **启动开发服务器**
   ```bash
   npm run dev
   ```

2. **打开浏览器**
   ```
   http://localhost:3000
   ```

3. **显示风场**
   - 点击右侧工具栏的 **风场按钮**（箭头图标）
   - 首次点击会加载数据（需要几秒钟）
   - 加载完成后会自动显示风场效果
   - 再次点击可以切换显隐

### 方法2：使用测试页面

1. **启动开发服务器**
   ```bash
   npm run dev
   ```

2. **打开测试页面**
   ```
   http://localhost:3000/test-wind-simple.html
   ```

3. **点击"显示风场"按钮**
   - 页面会显示加载状态
   - 加载完成后自动显示风场
   - 可以看到白色的风场粒子流动

## 🔍 问题排查

### 问题1：点击按钮没有反应

**可能原因**：
1. 插件未正确加载
2. 数据文件不存在
3. 浏览器控制台有错误

**解决方法**：
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页
3. 查找错误信息：
   - ❌ 如果看到 "Failed to fetch" → 数据文件路径错误
   - ❌ 如果看到 "Cannot find module" → 插件未安装
   - ❌ 如果看到 "WindLayer is not a constructor" → 插件版本不兼容

### 问题2：看不到风场效果

**检查清单**：
- [ ] 浏览器控制台是否有 "✅ 风场已显示" 的日志
- [ ] 是否飞到了正确的视角（太平洋区域）
- [ ] 粒子颜色是否为白色（可能与背景融合）
- [ ] 数据是否正确加载

**调试步骤**：
```javascript
// 在浏览器控制台输入：
console.log('windLayer:', windLayer);
console.log('windLayer.show:', windLayer?.show);
console.log('windLayer.isVisible:', windLayer?.isVisible);
```

### 问题3：风场加载很慢

**原因**：
- 数据文件较大（8.2MB）
- 需要转换数据格式
- 首次加载需要初始化 WebGL

**优化建议**：
1. 使用更小的数据文件
2. 预加载数据
3. 显示加载进度条

## 📊 预期效果

### 正常显示时应该看到：

1. **粒子流动**
   - 白色的小点在地球表面流动
   - 流动方向代表风向
   - 流动速度代表风速

2. **拖尾效果**
   - 粒子后面有淡淡的拖尾
   - 形成流线效果

3. **全球覆盖**
   - 覆盖全球范围（-180° 到 180°）
   - 南北纬 -90° 到 90°

### 控制台日志（正常情况）：

```
🔧 initWindLayer 被调用
   - viewer 存在: true
   - windLayer 已存在: false
🌬️ 开始加载全球风场数据...
⏳ 调用 loadGlobalWindData()...
✅ 全球风场数据加载成功
📊 风场数据详情: {...}
⏳ 创建 Cesium WindLayer 实例...
✅ cesium-wind-layer 插件加载成功
🔄 开始转换数据格式...
   - 输入数据点数: 64800
   - 网格尺寸: 361 x 181 = 65341 个点
   - 填充数据点: 64800
   - 覆盖率: 99.17%
✅ 数据转换完成
🌬️ 创建 WindLayer 实例...
✅ WindLayer 创建成功
✅ CesiumWindLayerWrapper 初始化完成
⏳ 正在显示风场...
✅ 风场已显示
```

## 🎨 调整参数

### 修改粒子数量

编辑 `src/components/MapContainer.vue`：

```javascript
windLayer = new CesiumWindLayerWrapper(viewer, windData, {
    particleSystemOptions: {
        maxParticles: 128 * 128,  // 改为 16384 个粒子（更密集）
        // 或
        maxParticles: 32 * 32,    // 改为 1024 个粒子（更稀疏）
    }
});
```

### 修改线宽

```javascript
particleSystemOptions: {
    lineWidth: 2.0,  // 更细的线
    // 或
    lineWidth: 8.0,  // 更粗的线
}
```

### 修改速度

```javascript
particleSystemOptions: {
    speedFactor: 0.5,  // 更慢
    // 或
    speedFactor: 2.0,  // 更快
}
```

### 修改拖尾长度

```javascript
particleSystemOptions: {
    fadeOpacity: 0.98,   // 短拖尾
    // 或
    fadeOpacity: 0.999,  // 长拖尾
}
```

## 🔧 技术细节

### 数据流程

```
1. 点击风场按钮
   ↓
2. 调用 toggleWindLayer()
   ↓
3. 检查 windLayer 是否存在
   ↓
4. 首次点击：调用 initWindLayer()
   ↓
5. 加载数据：loadGlobalWindData()
   ↓
6. 创建实例：new CesiumWindLayerWrapper()
   ↓
7. 异步初始化：_init()
   ↓
8. 动态导入插件：import('cesium-wind-layer')
   ↓
9. 转换数据格式：convertToWindLayerFormat()
   ↓
10. 创建 WindLayer：new WindLayer()
   ↓
11. 显示风场：windLayer.show = true
```

### 关键文件

- `src/components/MapContainer.vue` - 主组件，处理按钮点击
- `src/utils/cesiumWindLayerAdapter.js` - 适配器，封装插件
- `src/utils/windDataLoader.js` - 数据加载器
- `public/data/wind_data_0701.json` - 风场数据文件

## 📞 获取帮助

### 查看日志

1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 查找带有表情符号的日志：
   - 🔧 初始化相关
   - 🌬️ 风场相关
   - ✅ 成功信息
   - ❌ 错误信息

### 常见错误及解决

| 错误信息 | 原因 | 解决方法 |
|---------|------|---------|
| `Cannot find module 'cesium-wind-layer'` | 插件未安装 | `npm install` |
| `Failed to fetch /data/wind_data_0701.json` | 数据文件不存在 | 检查文件路径 |
| `WindLayer is not a constructor` | 插件版本不兼容 | 检查 Cesium 版本 |
| `viewer is not defined` | Cesium 未初始化 | 等待地图加载完成 |

## ✅ 验证清单

使用此清单确认风场功能正常：

- [ ] 开发服务器已启动
- [ ] 浏览器已打开应用
- [ ] 地图已加载完成
- [ ] 点击风场按钮
- [ ] 控制台显示 "✅ 风场已显示"
- [ ] 可以看到白色粒子流动
- [ ] 粒子有拖尾效果
- [ ] 再次点击可以隐藏风场
- [ ] 没有错误信息

## 🎉 成功标志

当你看到以下现象时，说明风场功能正常：

1. ✅ 白色粒子在地球表面流动
2. ✅ 粒子流动方向一致（代表风向）
3. ✅ 粒子有淡淡的拖尾
4. ✅ 覆盖全球范围
5. ✅ 相机移动时粒子跟随地球
6. ✅ 性能流畅（FPS > 50）

---

**更新时间**: 2024-12-15  
**版本**: v2.1  
**状态**: ✅ 已修复异步加载问题

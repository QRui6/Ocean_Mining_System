# 航点气象弹窗功能说明

## 功能概述

在航线动态演示过程中，当船舶正在行驶到某个航点时，会在该航点的上方自动弹出**下一个航段**的气象信息弹窗。同时，如果下一个航段存在高风险（警告或危险级别），会在右上角弹出风险警告提示。

### 自动关闭机制

1. **风险警告自动关闭**：当船舶完全通过预警区域（到达该航段的终点）后，右上角的风险警告会自动关闭
2. **航点弹窗自动关闭**：当船舶到达终点（完成整个航线）后，航点气象弹窗会自动关闭

## 显示逻辑

### 当前实现
- **弹窗位置**：显示在即将到达的航点（nextWp）上方
- **弹窗内容**：显示到达该航点后的下一个航段（到nextNextWp）的气象信息
- **触发时机**：船舶开始向某个航点移动时

### 示例说明
假设航线有航点：A → B → C → D，其中 B → C 航段有高风险

1. **船舶从 A 向 B 移动时**：
   - 航点弹窗位置：B 航点上方
   - 航点弹窗内容：B → C 航段的气象信息
   - 右上角警告：显示 B → C 航段的风险警告（因为有高风险）

2. **船舶到达 B 点后**：
   - 右上角警告：**自动关闭**（已通过预警区域）
   - 航点弹窗：更新为 C 航点上方，显示 C → D 航段信息

3. **船舶从 C 向 D 移动时**：
   - 航点弹窗位置：D 航点上方
   - 航点弹窗内容：无（因为没有下一个航段了）

4. **船舶到达 D 点（终点）后**：
   - 航点弹窗：**自动关闭**（航线完成）
   - 右上角警告：**自动关闭**（如果有的话）

## 双重提示系统

### 1. 航点气象弹窗（WaypointWeatherPopup）
- **位置**：航点上方
- **内容**：下一个航段的详细气象数据
- **样式**：紧凑的卡片式设计
- **颜色**：根据风险等级显示（绿/黄/橙/红）

### 2. 风险警告弹窗（RouteRiskWarning）
- **位置**：右上角固定位置
- **触发条件**：下一个航段风险等级为 warning 或 danger
- **关闭条件**：船舶通过该风险航段后自动关闭
- **内容**：警告信息和关键气象数据
- **样式**：醒目的警告面板

## 生命周期管理

### 航点气象弹窗
- **显示**：船舶开始向某个航点移动时
- **更新**：每到达一个航点，更新为下一个航段的信息
- **关闭**：船舶到达终点时自动关闭

### 风险警告弹窗
- **显示**：检测到下一个航段有高风险时
- **关闭**：船舶通过该风险航段后自动关闭
- **特点**：只显示当前或即将进入的风险区域，已通过的不再显示

## 实现细节

### 1. 新增组件

**文件：** `src/components/WaypointWeatherPopup.vue`

这是一个精简的气象弹窗组件，设计特点：
- 紧凑的卡片式设计（宽度240px，比原来减小约37%）
- 标题栏显示"下一航段气象"，清晰标注用途
- 显示航点名称
- 根据风险等级显示不同的边框颜色和样式
- 2x2网格布局，只显示最关键的4项气象数据：
  - 风速（m/s）
  - 风级（级）
  - 浪高（m）
  - 温度（°C）
- 精简的航段信息（距离和预计时间）
- 带有三角形指示器指向航点位置
- 平滑的淡入淡出动画效果

### 2. 核心方法

#### `showWaypointWeatherPopup(waypoint)` - routeDemoLayer.js
- **功能**：在航点上方显示气象弹窗
- **工作流程**：
  1. 计算航点的3D世界坐标
  2. 转换为屏幕2D坐标
  3. 考虑CSS缩放比例进行坐标调整
  4. 调用弹窗组件显示气象信息

### 3. 触发时机

#### 显示弹窗
在 `startAnimation()` 方法中，开始移动到下一个航点时：
```javascript
// 显示航点气象弹窗
if (nextNextWp) {
    this.showWaypointWeatherPopup(nextNextWp, nextWp);
    
    // 检查下一个航段是否有高风险，如果有则触发右上角警告
    if (nextNextWp.risk === 'warning' || nextNextWp.risk === 'danger') {
        this.showRiskWarning(nextNextWp);
    }
}
```

#### 关闭弹窗
在 `moveShipToWaypoint()` 回调中，到达航点后：
```javascript
// 1. 关闭通过的风险警告
if (nextWp.risk === 'warning' || nextWp.risk === 'danger') {
    this.closeRiskWarning();
}
```

在到达终点时：
```javascript
// 2. 关闭所有弹窗
this.closeWaypointWeatherPopup();
this.closeRiskWarning();
```

### 4. 核心方法签名

```javascript
/**
 * 显示航点气象弹窗
 * @param {Object} waypointData - 要显示的航点气象数据（下一个航段的终点）
 * @param {Object} positionWaypoint - 弹窗显示位置的航点（即将到达的航点）
 */
showWaypointWeatherPopup(waypointData, positionWaypoint = null)

/**
 * 显示右上角风险警告
 * @param {Object} waypoint - 航点数据
 */
showRiskWarning(waypoint)

/**
 * 关闭右上角风险警告
 */
closeRiskWarning()

/**
 * 关闭航点气象弹窗
 */
closeWaypointWeatherPopup()
```

### 5. 组件通信

通过全局引用实现：
- `window.appWaypointWeatherPopupRef` - 航点气象弹窗组件引用
- `window.appRiskWarningRef` - 风险警告组件引用
- 在 `App.vue` 的 `onMounted` 钩子中设置

### 6. 弹窗样式

#### 风险等级颜色方案
- **安全（safe）**：绿色边框和标识
- **注意（caution）**：黄色边框和标识
- **警告（warning）**：橙色边框和标识
- **危险（danger）**：红色边框和标识

#### 数值颜色编码
根据阈值动态显示颜色：
- 绿色：安全范围
- 黄色：接近阈值（80%）
- 橙色：达到阈值
- 红色：超过阈值（125%）

### 6. 显示内容

弹窗包含以下信息（精简版）：
1. **标题栏**："下一航段气象"标注和风险等级徽章
2. **航点名称**：显示目标航点名称
3. **气象数据网格**（2x2布局）：
   - 风速（m/s）
   - 风级（级）
   - 浪高（m）
   - 温度（°C）
4. **航段信息**：距离、预计时间

## 使用方式

1. 打开"航线动态"面板
2. 点击"开始演示"按钮
3. 船舶开始沿航线移动
4. 每到达一个航点，会在该航点上方自动弹出下一个航点的气象信息
5. 弹窗会自动跟随航点位置显示

## 技术要点

### 坐标转换
```javascript
// 1. 世界坐标 -> 屏幕坐标
const position = Cesium.Cartesian3.fromDegrees(lng, lat, 0);
const canvasPosition = Cesium.SceneTransforms.wgs84ToWindowCoordinates(
    viewer.scene,
    position
);

// 2. 考虑CSS缩放
const scaleX = window.innerWidth / 1920;
const scaleY = window.innerHeight / 1080;
const screenPosition = {
    x: canvasPosition.x * scaleX,
    y: canvasPosition.y * scaleY
};
```

### 定位样式
```css
transform: translate(-50%, -100%);  /* 居中并向上偏移 */
margin-top: -20px;  /* 额外向上偏移20px */
```

## 设计优化

### 精简版改进（v2.0）
- **尺寸缩小**：从380px减小到240px（减小约37%）
- **标题优化**：添加"下一航段气象"标注，更清晰
- **布局优化**：从3x2网格改为2x2网格
- **信息精简**：只保留最关键的4项气象数据
- **移除项目**：风向、能见度、航点描述
- **视觉优化**：更紧凑的间距和字体大小
- **性能提升**：更少的DOM元素，渲染更快

| 特性 | 原功能（探矿区域气象） | 新功能（航点气象弹窗） |
|------|---------------------|---------------------|
| 触发条件 | 检测航点附近的探矿区域 | 每个航点都触发 |
| 显示位置 | 屏幕底部固定位置 | 航点上方动态位置 |
| 显示内容 | 20天航程预报 + 7天到达后预报 | 下一航段气象数据 |
| 组件类型 | 大型面板 | 紧凑弹窗 |
| 设计风格 | 详细数据表格 | 简洁卡片式 |

## 注意事项

1. 弹窗位置会根据航点在屏幕上的实际位置动态调整
2. 考虑了CSS缩放，确保在不同分辨率下位置准确
3. 弹窗会在船舶移动到下一个航点时自动更新
4. 如果航点不在可见范围内，弹窗可能不会显示

## 后续优化建议

1. 添加弹窗自动隐藏功能（如5秒后自动消失）
2. 支持点击弹窗查看更详细信息
3. 添加弹窗位置智能调整（避免超出屏幕边界）
4. 支持同时显示多个航点的气象信息
5. 添加弹窗拖拽功能
